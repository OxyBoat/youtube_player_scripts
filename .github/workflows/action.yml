name: Update YouTube Player Script

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"   # every hour
      
permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  check-player:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: main  # Specify the base branch here

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install UV & deps
        run: |
          pip install uv
          uv sync

      - name: Run script and detect new player script
        id: run-script
        run: |
          set -eux

          # Capture list of directories before
          BEFORE=$(find . -maxdepth 1 -type d -not -name ".git" -exec basename {} \; | sort)

          # Run the Python script
          uv run workflow.py

          # Capture list of directories after
          AFTER=$(find . -maxdepth 1 -type d -not -name ".git" -exec basename {} \; | sort)

          # Find the new directory
          NEW_DIR=$(comm -13 <(echo "$BEFORE") <(echo "$AFTER") | head -n 1)

          if [ -z "$NEW_DIR" ]; then
            echo "No new directory created by script." >> "$GITHUB_OUTPUT"
            exit 1
          fi

          echo "✅ Detected new player directory: $NEW_DIR"
          echo "new_dir=$NEW_DIR" >> "$GITHUB_OUTPUT"

      - name: Set up Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create new branch and commit new directory
        run: |
          NEW_DIR="${{ steps.run-script.outputs.new_dir }}"
          BRANCH="add-player-${NEW_DIR}"

          git checkout -b "$BRANCH"
          git add -f "$NEW_DIR"
          git commit -m "chore: add new player script for $NEW_DIR"
          git push origin "$BRANCH"

      - name: Create PR if new directory exists
        if: steps.run-script.outputs.no_new_dir == 'false'
        run: |
          NEW_DIR="${{ steps.run-script.outputs.new_dir }}"
          BRANCH_NAME="add-player-${NEW_DIR}"

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create and push new branch
          git checkout -b "$BRANCH_NAME"
          git add "$NEW_DIR"
          git commit -m "chore: add new player script for $NEW_DIR"
          git push origin "$BRANCH_NAME"

          # Create PR
          gh pr create \
            --base main \
            --head "$BRANCH_NAME" \
            --title "Add player script for $NEW_DIR" \
            --body "This PR adds a new player script directory: \`$NEW_DIR\`"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
